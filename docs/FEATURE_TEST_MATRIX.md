# BOSS 海投助手功能清单与逐项验收矩阵

> 基线版本：v1.7.36
> 基线提交：2be3e52
> 开发目录：`/Users/gaohaizhen/Desktop/boss-dev`（`dev`）  
> 浏览器：ego-browser，加载 `boss-dev/extension`  
> 建立日期：2026-09-10

## 1. 目的与原则

本文档是插件的功能目录、测试边界和执行记录。功能清单以实际 UI、后台消息协议、content script、shared 模块和现有自动化测试交叉确认，不以旧 PRD 中尚未实现的规划项冒充已交付能力。

每个已实现功能都有独立编号。每个编号至少需要三类证据：

1. 静态/单元或 flow contract 检查：验证规则、状态转移、消息协议和安全门禁。
2. 浏览器独立场景：一次只验证一个功能，使用 ego-browser 观察实际页面、面板和日志。
3. 边界或回归结论：记录正常路径、异常输入、刷新/重载、重复操作或平台页面变化下的结果。

涉及真实 BOSS 沟通的场景遵守最小副作用原则：默认使用页面诊断、扫描预览和受控的 1 份测试；只有明确需要验证真实发送回执时才实际发送，并将本次上限调为 1。不会自动点击 BOSS 原生“发简历”按钮，也不会尝试验证码、平台限流或风控绕过。

## 2. 状态定义

| 状态 | 含义 |
|---|---|
| `待测` | 已列入范围，尚无本轮独立浏览器证据 |
| `通过` | 单元/flow、浏览器场景和边界检查均通过 |
| `问题` | 已发现可复现问题，需记录根因、修复和回归证据 |
| `阻塞` | 需要用户登录、验证码、平台临时限制或外部状态，不能安全绕过 |
| `不适用` | 该能力明确不在当前版本或产品边界内；保留边界测试记录 |

“单测/冒烟”列填写对应命令、测试名称或待补测试；“浏览器场景”必须是一个独立场景，不把多个功能的通过结果合并填入一个编号。

## 3. 自动化基线

| 层级 | 命令 | 当前基线 |
|---|---|---|
| 单元测试 | `npm test` | 139 个 unit tests + flow contract tests |
| 冒烟测试 | `npm run smoke` | 通过 |
| 发版检查 | `npm run release:check` | Release 文案一致性检查 |
| 浏览器 | ego-browser + 本矩阵 | 本轮逐项补录 |

基础回归每次修复后必须重新运行 `npm test && npm run smoke`。真实页面测试发现问题时，先在 `dev` 修复，再重新执行受影响编号和基础回归。

## 4. 功能总览

| 范围 | 已实现功能 | 规划/明确边界 |
|---|---|---|
| 入口与面板 | MV3 加载、BOSS URL 守卫、浮窗、弹出页、6 个面板、主题、帮助提示、版本守卫、新手引导 | 不支持非 BOSS 网站 |
| 任务与扫描 | 扫描预览、虚拟列表深扫、结果汇总、可解释原因、逐项勾选、投递一份、批量投递、目标模式、暂停/继续/跳过/停止、页面诊断 | 不绕过验证码/风控/平台上限 |
| BOSS 来源 | 推荐与具体求职期望识别、稳定期望 ID、普通筛选请求签名、刷新恢复与逐字段校验 | 不假设显示文字相同就代表已点击期望 |
| 筛选 | 职位/公司/JD OR、AND、NOT；地点包含/排除/精确；薪资；HR 活跃；猎头；外包；黑白名单 | 公司规模、行业、融资、经验、学历等 BOSS 原生筛选由 BOSS 页面负责，不是插件独立规则 |
| 消息 | BOSS 自动招呼读写与回读、插件总开关、多段消息、招呼/补充角色、变量、回执、会话匹配、幂等、草稿保护 | 不做全自动 AI 回复 |
| 简历 | 图片简历、多图顺序发送、压缩、预览、多个图片方案、默认方案、关键词绑定、发送时机 | 不自动点击 BOSS 原生“发简历”；附件简历自动发送未纳入当前 UI |
| 安全控制 | 本次/每日/同公司上限、HR 冷却、职位永不重复、连续失败暂停、定时投递、随机间隔 | 不以自动化绕过平台限制为目标 |
| 执行隔离 | 临时详情页沟通、左列表保护、消息页复用与防卡顿刷新、左右分屏、80% 缩放、窄屏回退 | 页面改版时仍需诊断与人工复核 |
| 数据与可观察性 | 本地存储、任务检查点、历史、每日统计、实时日志、详细调试日志、配置导入导出、更新检查 | 配置/日志可能含岗位和聊天文字，导出前需人工检查 |
| 产品规划边界 | — | 多套完整求职方案、投递漏斗、语义匹配、AI 回复、跟进提醒暂不作为当前版本已实现功能 |

## 5. 逐项测试矩阵

### 5.1 入口、连接与面板基础

| ID | 功能点 | 期望 | 单测/冒烟 | 独立浏览器场景 | 状态 | 证据/问题 |
|---|---|---|---|---|---|---|
| ENT-01 | MV3 扩展加载 | manifest 可加载，版本与运行时一致 | `manifest version + version guard` | ego 扩展页重载后显示 `1.7.36`；当前 BOSS 面板显示 `v1.7.36` 且无版本锁定 | 通过 | ego 扩展页、当前 iframe URL `v=1.7.36`、连接徽标 |
| ENT-02 | BOSS URL 守卫 | BOSS 列表/聊天页可连接，非 BOSS 页不执行 | `boss-url` tests | BOSS 列表显示“已连接 BOSS · v1.7.36”；打开 `example.com` 后弹出页锁定为“请前往 BOSS 直聘页面使用插件”，危险按钮未执行 | 通过 | 2026-09-11 ego；BOSS 与非 BOSS 两个独立场景 |
| ENT-03 | 浮窗打开与复用 | BOSS 页右侧浮窗可打开/关闭，重复打开不重复注入 | popup/content smoke | 列表页浮窗根节点存在，打开后只有一个 `#bht-frame`，iframe 为 `mode=float&v=1.7.36` | 通过 | 当前 BOSS DOM/Target.getTargets |
| ENT-04 | 弹出页入口 | popup 能识别当前 BOSS 页并打开浮窗，非 BOSS 页给出引导 | popup smoke | 职位列表页真实 popup 显示“已在 BOSS 页面”并打开 `mode=float&v=1.7.36` 浮窗；聊天页明确引导到职位列表；非 BOSS 页显示通用引导 | 通过 | 2026-09-11 ego；真实 popup 列表/聊天/非 BOSS 三路径 |
| ENT-05 | 面板六个 tab | 任务、筛选、消息、简历、记录、设置切换且表单不串页 | DOM/flow panel tests | 独立点击 6 个 tab；每个目标 tab 和 panel 均 active，最后恢复任务页 | 通过 | v1.7.36 iframe DOM |
| ENT-06 | 明暗主题 | light/dark 切换立即生效并持久化 | panel theme tests | light 设置为 `data-theme=light`，dark 恢复为 `data-theme=dark` | 通过 | v1.7.36 iframe DOM |
| ENT-07 | 帮助提示 | 标题旁帮助入口显示对应说明，不遮挡主要控件 | panel DOM checks | 打开任务控制说明，popover 可见且内容为对应说明 | 通过 | v1.7.36 iframe DOM |
| ENT-08 | 版本守卫与页面刷新提示 | UI、后台、content 版本不一致时锁定危险操作并提示刷新 | mixed-version flow test | 扩展页重载后实际代码为 `1.7.36`；查询参数不能伪造旧代码，旧脚本混载由 flow contract 覆盖 | 通过（代码/flow） | 需保留旧 content 页面才能做真实混载；当前 flow contract 已覆盖锁定 |
| ENT-09 | 新手引导 | 首次显示、完成后不重复；设置页可重看 | onboarding flow test | 设置页重看引导，逐步通过 7 个步骤；完成后 overlay 消失并写入 `bht_onboarding.completed=true` | 通过 | 2026-09-11 ego；未修改业务配置 |

### 5.2 任务、扫描和预览

| ID | 功能点 | 期望 | 单测/冒烟 | 独立浏览器场景 | 状态 | 证据/问题 |
|---|---|---|---|---|---|---|
| TASK-01 | 页面诊断 | 返回页面类型、关键选择器、content 版本和可操作性 | diagnose flow contract | 在职位列表点“页面诊断”，返回当前列表 URL、48 张卡片、标题节点和样例岗位；连接与日志正常 | 通过 | v1.7.36 ego 面板 |
| TASK-02 | 扫描预览只读 | 扫描不会发送沟通，完成后形成待确认任务 | preview flow tests | 本轮扫描 48 岗，建立 45 个待投队列；成功/处理均为 0，无聊天发送副作用 | 通过 | 2026-09-11 ego；面板实时日志 |
| TASK-03 | 虚拟列表深扫 | 持续滚动到真实底部，保留中间窗口岗位，不在旧轮次上限提前结束 | preview scan contracts | 2026-09-11 ego 扫描 450 岗，滚动到 `scrollTop=66738`、`scrollHeight=67922`、74 轮后到达底部，未改变求职期望来源 | 通过 | 2026-09-11 ego；长列表中间窗口与底部均有结果 |
| TASK-04 | 扫描进度与截止收尾 | 60 秒预算、取消和最后一批结果不会回退已采集岗位 | preview timeout contracts | 独立边界页验证过期截止时间进入 `timeout`，截止前采集结果可在 finalization window 内保留 | 通过 | 2026-09-11 ego task 160 `boundary-audit.html`；真实账号取消路径另见 TASK-11 |
| TASK-05 | 预览汇总 | 显示扫描总数、通过数、排除数、原因计数和风险提示 | summary/reason tests | 汇总显示扫描 48、通过 45、排除 3，并提示通过率超过 80%；与 48 条明细一致 | 通过 | 2026-09-11 ego |
| TASK-06 | 明细可解释原因 | 每个岗位显示职位、公司、地点、薪资、决定和原因 | filter/reason tests | 明细显示职位/公司/地点/薪资；3 条排除分别显示已处理、JD 排除词、职位排除词 | 通过 | 2026-09-11 ego |
| TASK-07 | 预览勾选 | 通过项默认勾选，可逐个取消；取消后不能进入投递队列 | queue flow tests | 45 个通过项默认勾选；取消一个后变 44，恢复后回到 45，未启动投递 | 通过 | 2026-09-11 ego |
| TASK-08 | 全选通过项 | 全选/取消全选只影响通过项，不会选中拒绝项 | panel/queue tests | 全选关闭后 45→0，开启后 0→45；3 个拒绝项始终没有 checkbox | 通过 | 2026-09-11 ego 面板；未启动投递 |
| TASK-09 | 通过率风险提示 | 极低或极高通过率提示检查筛选，不能阻止用户查看结果 | preview UI tests | 宽规则扫描 420/440 通过并显示高通过率提示；临时不命中关键词扫描 0/440，任务仍保留 440 条排除明细 | 通过 | 2026-09-11 ego；高/低两端均未阻止查看结果 |
| TASK-10 | 扫描自动回顶部 | 扫描结束列表回到顶部，不丢原始可见岗位 | scan navigation tests | 扫描结束后 BOSS 列表 `scrollY=0`，首批岗位仍可见 | 通过 | 2026-09-11 ego |
| TASK-11 | 扫描取消 | 取消只终止当前扫描，保留上一批预览，不发送消息 | cancellation flow tests | 启动预览后在 `locating_list` 阶段停止；旧任务仍为 `completed`、26 条结果，历史 40 条不变 | 通过 | 2026-09-11 ego；`previewCancelled=true` |
| TASK-12 | 扫描非列表页恢复 | 从 BOSS 详情/聊天页点扫描时自动回到安全列表或给出明确失败 | navigation recovery tests | BOSS 聊天页 popup 不再伪装成可打开面板，而是明确引导到职位列表；内部扫描恢复仍能自动导航到安全列表，且不会退回外部历史页 | 通过 | 2026-09-11 ego + flow；聊天页入口清晰失败，详情/非列表恢复路径受保护 |

### 5.3 插件筛选规则

| ID | 功能点 | 期望 | 单测/冒烟 | 独立浏览器场景 | 状态 | 证据/问题 |
|---|---|---|---|---|---|---|
| FIL-01 | 职位 OR | 命中任一关键词通过，全部未命中拒绝 | filter-engine tests | `运维,客服` 扫描：命中项通过，`网络工程师` 等未命中项显示未命中原因 | 通过 | 2026-09-11 ego |
| FIL-02 | 职位 AND | 必须同时命中全部关键词 | filter-engine tests | `运维,工程师` 扫描仅保留同时命中两词的 6 岗，缺词显示 AND 原因 | 通过 | 2026-09-11 ego |
| FIL-03 | 职位 NOT | 命中任一排除词立即拒绝，优先于 OR/AND | filter-engine tests | `运维` 排除词命中的岗位显示“职位名称命中排除词” | 通过 | 2026-09-11 ego |
| FIL-04 | 规则开关保留输入 | 关闭 OR/AND/NOT 只忽略该组，不清空输入 | filter toggle tests | 关闭 OR 后 storage 记录 `enabled=false`，重新开启仍保留 `保留词` | 通过 | 2026-09-11 ego |
| FIL-05 | 公司 OR/NOT | 公司包含和公司排除独立生效 | filter-engine tests | `新华三` 包含仅保留新华三；`美团` 排除命中公司原因 | 通过 | 2026-09-11 ego |
| FIL-06 | JD OR/AND/NOT | JD 三类规则按预期判断并显示原因 | filter-engine tests | JD OR `云平台/网络运维`、AND `网络运维/系统运维`、NOT `系统运维` 三次扫描均按原因区分 | 通过 | 2026-09-11 ego |
| FIL-07 | 关键词分隔与标准化 | 中英文逗号、顿号、斜杠、反斜杠、换行；大小写/空白/标点标准化 | text/filter tests | 输入 `/`、顿号、换行、逗号、分号、竖线混合关键词，保存后拆为 7 个词，重载后按逗号回填 | 通过 | 2026-09-11 ego；临时词已清空并重载确认 |
| FIL-08 | 地点包含 | `广州` 可命中 `广州·天河区`，空值表示不限 | location tests | `越秀` 包含扫描通过岗位地点均为 `广州·越秀区·北京路` | 通过 | 2026-09-11 ego |
| FIL-09 | 地点排除 | 命中排除地点的岗位拒绝 | location tests | 排除 `番禺` 后 9 条明细命中地点排除原因 | 通过 | 2026-09-11 ego |
| FIL-10 | 地点精确匹配 | 精确模式不把区县扩展文本当作完全相等 | location tests | 精确匹配 `广州` 后 0 通过、46 条地点未命中；与包含语义不同 | 通过 | 2026-09-11 ego |
| FIL-11 | 薪资下限/上限 | 以月薪区间比较；低于下限或高于上限拒绝 | salary/filter tests | 月薪 9000–10000 扫描 26 通过；低薪原因 19，区间重叠岗位按区间规则保留 | 通过 | 2026-09-11 ego |
| FIL-12 | HR 活跃上限 | 在线到所选上限通过，未知/超限拒绝或待投递时核对 | active tests | `在线` 扫描：刚刚/3 日内明文活跃岗位被排除；列表无活跃文案的岗位保留到投递时核对 | 通过 | 2026-09-11 ego；未启动投递 |
| FIL-13 | 猎头排除 | 明确猎头标签/名称或金牌猎头标识时拒绝 | hunter tests | 真实 440 岗扫描出现 `FILTER_HUNTER=1`，该岗位明细标记猎头排除；开关重载后仍保持开启 | 通过 | 2026-09-11 ego；摘要含 `FILTER_HUNTER: 1` |
| FIL-14 | 外包/驻场排除 | 职位、公司或 JD 命中特征时拒绝 | outsource tests | 真实 440 岗扫描出现 `FILTER_OUTSOURCE=1`，两个“驻场运维工程师”显示“职位名称命中排除词：驻场”；开关重载后仍保持开启 | 通过 | 2026-09-11 ego；摘要含 `FILTER_OUTSOURCE: 1` 与标题排除原因 |
| FIL-15 | 公司黑名单 | 命中黑名单优先拒绝并显示公司原因 | blacklist tests | 黑名单 `新华三` 后 6 条岗位均显示公司黑名单原因 | 通过 | 2026-09-11 ego |
| FIL-16 | 公司白名单 | 白名单可保存；仅投白名单开启后只保留命中公司 | whitelist tests | 仅投白名单 `新华三` 后 5 个通过岗位公司均为新华三 | 通过 | 2026-09-11 ego |
| FIL-17 | 筛选保存持久化 | 保存后刷新面板/页面，字段和开关恢复 | storage/filter tests | 混合分隔符筛选保存后重载面板，回读值和后台数组一致；随后恢复默认筛选 | 通过 | 2026-09-11 ego |

### 5.4 求职期望、来源恢复与列表保护

| ID | 功能点 | 期望 | 单测/冒烟 | 独立浏览器场景 | 状态 | 证据/问题 |
|---|---|---|---|---|---|---|
| SRC-01 | 推荐来源识别 | 推荐入口记录为 `recommend`，刷新恢复不误点具体期望 | source-context tests | 当前 BOSS 列表请求 `encryptExpectId=`，DOM active count=1，来源为 `recommend`、标签为“推荐” | 通过 | 2026-09-11 ego；直接读取 content source context |
| SRC-02 | 具体求职期望识别 | 记录稳定 ID/encryptId，不只记录显示文字 | expectation tests | 进入“运维/技术支持(广州)”后记录 `sourceType=expectation`、稳定 `expectationKey` 与匹配请求 | 通过 | 既有 ego 证据：`12bc20ce8ccfd8790nBy3du0E1ZZ`，DOM active count=1 |
| SRC-03 | 多求职期望区分 | 多个期望使用 key 精确匹配，不能只按相似文本选择 | expectation tests | 同一 BOSS tab 依次点击“推荐”和“运维/技术支持(广州)”；前者为 `recommend`/空 key，后者为 `expectation`/`12bc20ce8ccfd8790nBy3du0E1ZZ`，两次 DOM active count 均为 1 且请求证据匹配 | 通过 | 2026-09-11 ego；两来源 key 精确区分 |
| SRC-04 | 普通筛选请求签名 | 保存 city/query/experience/degree/salary/industry/scale 等有效请求字段 | filter-context tests | BOSS 原生选择“工作经验=1年以内”后请求为 `experience=103`；扫描快照写入 `hints/request`，且 `requestMatches=true`；清空后刷新恢复无普通筛选 | 通过 | 2026-09-11 ego；`encryptExpectId` 与具体期望 key 同时匹配 |
| SRC-05 | 来源恢复点击精确性 | 恢复时只点击准确的入口/选项，不把搜索词、推荐标签误当期望 | restore flow tests | “运维/技术支持(广州)”刷新恢复日志显示精确期望 key，未按显示词猜测 | 通过 | 既有 ego 证据：`刷新并继续` 恢复验证通过 |
| SRC-06 | 恢复逐字段校验 | 恢复后逐项验证期望和普通筛选；失败则暂停/报错，不盲投 | restore/filter tests | 刷新后验证期望和普通筛选，日志为“求职期望和普通筛选恢复验证通过” | 通过 | 既有 ego 证据；恢复后新扫描 15 个岗位 |
| SRC-07 | 刷新后列表去重 | 新批次与历史预览按 jobId 或公司+职位合并，不重复入队 | task-model tests | 刷新续投批次统计为新增 11、重复 4；本轮真实刷新从 802 合并到 1064，新增 262、重复 188；目标模式/单测均按稳定 jobId 合并 | 通过 | 既有 ego + 2026-09-11 修复后 ego + `task-model` flow contract |
| SRC-08 | 左列表保护 | 预览和正式投递不通过左卡片执行沟通；列表 URL、文档实例和岗位集合保持 | worker/list preservation tests | 真实 1 份沟通使用临时详情页，日志确认左列表筛选、滚动、页面实例未变化 | 通过 | 既有 ego 证据；执行页触发沟通 |
| SRC-09 | 目标模式来源门禁 | 没有可恢复来源时禁止启动目标模式，明确要求重新扫描 | target flow tests | BOSS 浮窗中将来源清空后开启目标模式并点批量；任务仍为 stopped，成功计数不变，未进入投递循环 | 通过 | 2026-09-11 ego；`JOB_SOURCE_NOT_RESTORABLE` 门禁生效 |

### 5.5 消息与会话

| ID | 功能点 | 期望 | 单测/冒烟 | 独立浏览器场景 | 状态 | 证据/问题 |
|---|---|---|---|---|---|---|
| MSG-01 | BOSS 招呼状态同步 | 显示账号全局开关、当前话术、同步时间和错误状态 | greeting flow tests | 消息页点击“同步状态”后仍显示账号开关、当前话术、同步时间，状态为已开启 | 通过 | 2026-09-11 ego；原账号状态已恢复 |
| MSG-02 | BOSS 招呼开关二次确认 | 修改前确认，修改后回读；回读不一致不提示成功 | greeting control tests | 取消关闭保持开启；确认关闭后回读“已关闭 · 推荐”，随后确认开启并恢复 | 通过 | 2026-09-11 ego；仅测试账号 |
| MSG-03 | 编辑 BOSS 当前话术 | 最多 100 字，保存后成为当前话术并回读 | greeting save tests | 保存临时短话术后回读，再恢复原话术；字符计数正常 | 通过 | 2026-09-11 ego；未发送真实消息 |
| MSG-04 | 插件文字总开关 | 关闭后不发送任何插件文字 | message planner tests | 消息页关闭总开关后流程预览只保留 BOSS 原生招呼，显示“插件已关闭”；保存回读 `pluginTextEnabled=false`，恢复后回读 `true` | 通过 | 2026-09-11 ego + unit/flow；未为此项新增真实沟通 |
| MSG-05 | 多段消息增删改 | 可新增、删除、编辑和启停消息段 | template flow tests | 新增临时段、编辑、禁用、删除并重载；最终恢复两段模板 | 通过 | 2026-09-11 ego |
| MSG-06 | 消息段角色 | 每段可设招呼/补充，顺序按页面顺序 | greeting-policy tests | “补充→招呼”保存后回读成功；删除另一段时也保留未保存角色修改 | 通过 | 2026-09-11 ego；修复 `app.js` 删除重绘覆盖草稿问题 |
| MSG-07 | 消息变量渲染 | HR、职位、公司、技能、城市替换；缺失变量阻止裸占位符发送 | template/unit tests | 真实测试先故意加入未绑定的 `{匹配技能}`，会话已打开但发送前暂停；修正模板后实际发送“测试变量：运维工程师｜颐能软件｜广州·黄埔区·大沙｜曾女士”，收到 `TEXT_SENT` | 通过 | 2026-09-11 ego + unit；原模板已恢复 |
| MSG-08 | 自动招呼已发送 | 平台回执确认已发送时跳过所有 greeting，只发 supplement | greeting-policy tests | 已有真实回执日志显示检测到 BOSS 已发送招呼，随后仅准备/发送第 2 段并收到确认 | 通过 | 2026-09-11 ego；未新增消息 |
| MSG-09 | 自动招呼未发送 | 未发送时按页面顺序发送全部启用段 | greeting-policy tests | 临时关闭 BOSS 自动招呼后，对“机房运维”真实测试显示 `not_sent`，插件按第 1 段招呼→第 2 段补充顺序各收到 `TEXT_SENT`；随后恢复 BOSS 开关 | 通过 | 2026-09-11 ego；账号设置已恢复 |
| MSG-10 | 自动招呼未知保护 | 平台和 DOM 均无法确认时，含 greeting 模板必须暂停，不冒险发送 | greeting-policy tests | 独立边界页构造空证据，实际调用 greeting policy 得到 `NATIVE_GREETING_UNKNOWN`，计划被阻止 | 通过 | 2026-09-11 ego task 160 `boundary-audit.html`；未向真实 HR 发送未知状态消息 |
| MSG-11 | 会话匹配 | 以公司+HR+职位优先；歧义时暂停，不发错会话 | conversation-match tests | 真实日志出现 `company+hr` 会话打开确认并带 `[送达]`，公司、HR 和岗位均与当前任务一致 | 通过 | 既有 ego 真实沟通 + conversation-match tests |
| MSG-12 | 文本发送回执 | 只有出现本人新消息气泡才算成功，输入框填充不算成功 | delivery receipt tests | 真实日志连续出现“第 2 段发送确认”和“已发送第 2 段”，历史成功才在回执后写入 | 通过 | 既有 ego 真实沟通；未新增消息 |
| MSG-13 | 消息草稿持久化 | 仅实际编辑触发保存；面板刷新/切 tab/IME 组合态不覆盖已保存内容 | autosave tests | 编辑临时消息，等待自动保存后切 tab/重载回读；最终用可见消息控件恢复原文 | 通过 | 2026-09-11 ego；自动保存防抖为 1.8 秒 |
| MSG-14 | 模板保存竞态 | 保存中继续编辑，旧请求完成不能覆盖新草稿 | autosave contract | 保存 A 后立即编辑为 B，等待保存链完成后回读为 B；随后轮询保存恢复原文 | 通过 | 2026-09-11 ego；旧请求未覆盖最新草稿 |

### 5.6 简历方案与图片发送

| ID | 功能点 | 期望 | 单测/冒烟 | 独立浏览器场景 | 状态 | 证据/问题 |
|---|---|---|---|---|---|---|
| RES-01 | 图片导入 | 只接受图片；单张超过 8 MB 拒绝或提示 | resume storage tests | 真实导入 `assets/logo.png` 后缩略图为 1、保存成功；`8,388,609` bytes PNG 被提示“没有成功导入任何图片”，存储保持 0 | 通过 | 2026-09-11 ego；临时文件已清理 |
| RES-02 | 图片压缩与本地存储 | 导入时压缩，保存在本地扩展存储，不上传作者服务器 | resume tests/privacy review | 保存后回读 `logo.png` data URL；重载面板缩略图仍为 1，数据只在 `bht_resumes` 本地存储 | 通过 | 2026-09-11 ego；未触发作者域请求 |
| RES-03 | 多图顺序 | 缩略图按显示顺序串行发送，不能并发乱序 | resume send tests | 导入两张不同尺寸 PNG，保存/重载后保持 `2` 张；真实 1 份沟通在文本完成后按日志 `图片简历已发送 1/2`、`2/2` 顺序完成，随后清空方案 | 通过 | 2026-09-11 ego；真实串行发送证据 |
| RES-04 | 图片预览灯箱 | 点击缩略图可全屏，左右切换，Esc 关闭 | lightbox tests | 保存测试图片后点击缩略图，打开 `image-preview.html` 弹窗；弹窗关闭后无残留 | 通过 | 2026-09-11 ego；单图场景 |
| RES-05 | 清空图片 | 清空后 UI 和本地方案都无图片，保存后刷新仍为空 | resume flow tests | 清空测试图片、保存，后台和缩略图均为 0，再删除临时方案 | 通过 | 2026-09-11 ego |
| RES-06 | 多个简历方案 | 可新建、重命名、切换；至少保留一个可用方案 | profile tests | 新建/重命名/保存临时方案，新增绑定规则，删除方案后回到唯一默认方案 | 通过 | 2026-09-11 ego |
| RES-07 | 默认方案 | 设为默认后无绑定命中时选择该方案 | profile/binding tests | 临时方案设为默认后回读 profileId，切回默认方案并删除临时方案，最终恢复 `default` | 通过 | 2026-09-11 ego；未触发投递 |
| RES-08 | 绑定规则 | 职位/JD 关键词命中规则，优先级数字越小越先；无命中回退默认 | binding tests | 保存临时“运维/技术支持”绑定规则，回读关键词和 profileId，删除方案后规则同步删除 | 通过 | 2026-09-11 ego；优先级冲突仍由 unit/flow 覆盖 |
| RES-09 | 自动发送时机 | 关闭、仅手动、文本完成后自动三种策略边界明确 | resume-policy tests | UI 临时切换为关闭+手动，回读后恢复开启+`after_text`；实际发送策略由 unit/flow 覆盖 | 通过 | 2026-09-11 ego；未发送 |
| RES-10 | 图片发送回执与重复保护 | 只有 IMAGE_SENT 回执算成功；重试不重复已发图片 | image receipt/idempotency tests | 真实 1 份沟通完成 `2/2` 图片发送并只增加 1 条成功历史；IMAGE_SENT 回执、失败重试幂等由 unit/flow 覆盖 | 通过 | 2026-09-11 ego + unit/flow；未额外制造失败重试副作用 |
| RES-11 | BOSS 原生简历边界 | 不自动点击平台“发简历”，消息后仅按图片策略发送 | ADR-0011 + flow tests | 真实沟通日志只有图片策略安全跳过/发送路径，未出现自动点击 BOSS 原生“发简历” | 通过 | 既有 ego + ADR-0011；未点击平台原生简历按钮 |
| RES-12 | 附件简历自动发送边界 | 当前 UI 不提供附件自动发送入口，必须明确显示不适用 | scope check | 简历页只有图片简历入口，发送策略只有图片/手动选项，后台已删除附件自动发送设置 | 通过 | 2026-09-11 ego + storage normalization |

### 5.7 投递循环、目标模式、限额和计划

| ID | 功能点 | 期望 | 单测/冒烟 | 独立浏览器场景 | 状态 | 证据/问题 |
|---|---|---|---|---|---|---|
| RUN-01 | 投递一份 | 只取一个未处理通过岗位，后续再次点击不重复 | test-delivery/queue tests | 本轮连续两次各只取一个未处理岗位，分别完成变量和双消息回执；第二轮暴露旧分支漏记 `processed`，已修复为成功数与处理数同步并由 flow 回归覆盖 | 通过 | 2026-09-11 ego + 修复后 unit/flow；未再追加第三份真实沟通 |
| RUN-02 | 批量投递确认门禁 | 仅对预览勾选项建队列；无预览/无通过项不能启动 | start/queue tests | 临时清空当前任务后重载：批量投递和投递一份均禁用，扫描按钮仍可用；随后恢复 26 条预览 | 通过 | 2026-09-11 ego；未触发投递 |
| RUN-03 | 单岗位完整循环 | 详情→沟通→会话→文本→图片→记录→下一岗顺序正确 | delivery flow contracts | 真实测试账号完成 1 份沟通：会话回执、补充文本回执、图片无图安全跳过、历史成功写入 | 通过 | 既有 ego 证据；成功 1、失败 0、跳过 0 |
| RUN-04 | 目标模式启动 | 设置 1-500 成功目标，批量投递按成功数运行 | target flow tests | 目标设为 1，成功计数达到 `1/1` 后自动停止；目标输入边界 1/500 也已验证 | 通过 | 既有 ego + 2026-09-11 ego |
| RUN-05 | 目标模式补充刷新 | 当前批次耗尽且未达目标时刷新列表、恢复来源、扫描、去重、继续 | target refresh tests | 安全夹具目标 1、0 个可投岗位真实进入刷新路径；恢复 `运维/技术支持(广州)` 与请求签名通过，刷新扫描新增 313、重复 137，随后无新增可投安全停止；修复后真实刷新在同一面板完成新增 262、重复 188 的合并 | 通过 | 2026-09-11 ego；未发送真实消息，原任务已恢复 |
| RUN-06 | 目标模式成功口径 | 只有成功沟通计入目标，跳过/失败不计入 | task-model tests | 独立边界页验证 `success=1, skipped=1, failed=1` 时仍剩 2 个目标；成功到 3 才达到目标；面板 UI 同时显示 `成功 1/2` | 通过 | 2026-09-11 ego task 160 `boundary-audit.html` + panel harness；真实目标沟通已验证成功路径 |
| RUN-07 | 目标模式幂等启动 | 目标已达到或任务运行中重复点批量不重启/不重置 | target flow tests | 已完成目标后重复启动返回 `alreadyCompleted=true`，未新增历史或消息 | 通过 | 既有 ego 证据 |
| RUN-08 | 暂停 | 在安全步骤暂停，当前检查点持久化，不能自动继续 | task control tests | 合成无发送控制任务经真实按钮暂停，刷新后仍为 `paused`，原因为“用户暂停任务”，后台 runner 未继续 | 通过 | 2026-09-11 ego；未触发真实岗位 |
| RUN-09 | 继续 | 从保存游标继续，不重发已完成段/图片 | recovery/idempotency tests | 合成已跳过游标点“继续”，队列直接完成，岗位状态和计数不回退、不重复 | 通过 | 2026-09-11 ego；真实 runner 状态持久化检查 |
| RUN-10 | 跳过当前 | 当前岗位标记 skipped，队列进入下一岗；暂停时也不遗留 sticky skip | control tests | 真实暂停态点“跳过”，当前项为 `SKIPPED`、队列为 `skipped`、跳过计数为 1，随后安全完成 | 通过 | 2026-09-11 ego；无 sticky skip、未发送消息 |
| RUN-11 | 停止 | 取消页面操作、保存 stopped 终态，不被迟到快照覆盖 | stop/cancellation tests | 合成运行任务点“停止”并快速刷新，终态为 `stopped` 且 completion signal 为 `confirmed`，runner 不再运行 | 通过 | 2026-09-11 ego；未触发真实岗位 |
| RUN-12 | 失败重试 | 只重试当前失败步骤/可安全环境错误，不重复点击已创建会话 | retry tests | 独立边界页实际调用环境错误分类：`CONTENT_INJECT_FAIL` 可自动继续、`WORKER_PAGE_NOT_READY` 可重试，取消和 HR 筛选不重试；面板暂停弹窗点击重试传出 `{retry:true}` 并恢复运行 | 通过 | 2026-09-11 ego task 160 `boundary-audit.html` + panel harness；未对真实账号故意制造失败 |
| RUN-13 | 连续失败暂停 | 达到阈值自动暂停并显示具体原因 | failure counter tests | 独立边界页读取已发布后台的阈值与 `EXEC_CONSECUTIVE_FAIL` 分支；面板注入 3 次失败暂停状态后展示具体原因和暂停控件 | 通过 | 2026-09-11 ego task 160 `boundary-audit.html` + panel harness；未改真实账号阈值制造失败 |
| RUN-14 | BOSS 每日沟通上限 | 识别平台上限弹窗后直接停止，不反复重试 | daily-limit tests | 独立边界页读取 content 的上限检测和后台 `BOSS_DAILY_LIMIT` 终止分支；面板以 stopped 终态展示，不进入重试 | 通过 | 2026-09-11 ego task 160 `boundary-audit.html` + panel harness；未主动触发平台风控/上限 |
| RUN-15 | 定时投递 | 运行日、时段、跨边界暂停和闹钟恢复正确 | schedule tests | 独立边界页实际调用定时模块验证 `09:30` 可投、`12:00` 自动指向 `14:00`；面板 harness 显示定时暂停原因和自动恢复提示 | 通过 | 2026-09-11 ego task 160 `boundary-audit.html` + panel harness；真实窗口外启动和非法时段另有记录 |
| RUN-16 | 随机投递间隔 | 基准值在允许范围，等待期间状态展示正确 | schedule/unit tests | 临时设置基准 1 秒，后台规范化为 `[1000,2000]`；恢复默认 `[4000,6000]` | 通过 | 2026-09-11 ego；运行中倒计时由已有 flow 覆盖 |
| RUN-17 | 本次/每日上限 | 达到本次或每日上限停止/提示，0 每日上限表示未设上限 | limit tests | 临时设置本次上限 1、每日上限 0，回读准确；随后恢复 30/80，真实目标 1 已停止 | 通过 | 2026-09-11 ego + 既有真实受控任务 |
| RUN-18 | 同公司上限 | 同公司达到日上限后后续岗位预览拒绝/跳过 | dedup/limit tests | 同一真实扫描批次注入本地 `byCompany=1`、上限 `1` 后复扫，目标岗位变为 `DEDUP_COMPANY_DAILY`，显示 `1/1` | 通过 | 2026-09-11 ego；使用浏览器本地日期键 |
| RUN-19 | HR 冷却 | 冷却期内同 HR 不重复沟通，0 表示不冷却 | dedup tests | 同一真实扫描批次注入同 HR 的历史成功记录、关闭职位重复后复扫，目标岗位变为 `DEDUP_BOSS`，显示 30 天 | 通过 | 2026-09-11 ego；未发送消息 |
| RUN-20 | 永不重复职位 | jobId/幂等记录/历史成功任一证据命中即拒绝 | dedup tests | 同一真实扫描批次注入目标岗位成功历史后复扫，目标岗位变为 `DEDUP_JOB`，不进入队列 | 通过 | 2026-09-11 ego；未发送消息 |
| RUN-21 | 重新发布策略 | 默认不放行；开启 allowRepublishedJob 且 securityId 变化时才允许 | dedup tests | 同一 jobId 使用旧 `securityId`：默认拒绝；开启 `allowRepublishedJob` 后当前 securityId 变化则恢复通过 | 通过 | 2026-09-11 ego；未对真实岗位重复投递 |

### 5.8 分屏、执行页和页面稳定性

| ID | 功能点 | 期望 | 单测/冒烟 | 独立浏览器场景 | 状态 | 证据/问题 |
|---|---|---|---|---|---|---|
| LAY-01 | 自动左右分屏 | 列表在左，消息在右，两个窗口边界可读 | window layout tests | 真实受控任务打开列表/消息双窗口，消息页可操作且左侧列表保持 | 通过 | 既有 ego 证据 |
| LAY-02 | 80% 缩放 | 分屏窗口按 80% 缩放，面板和页面控件仍可操作 | window/smoke tests | 受控任务消息页缩放为 `0.8`，文本回执仍能确认 | 通过 | 既有 ego 证据 |
| LAY-03 | 窄屏/拒绝缩放回退 | 浏览器不支持窗口调整时回退普通标签，不丢列表 tab | layout fallback tests | 独立边界页实际调用窗口布局模块：`900px` 返回安全 fallback，`1920px` 双栏不重叠且边界匹配 | 通过 | 2026-09-11 ego task 160 `boundary-audit.html`；真实浏览器拒绝窗口 API 未强行制造 |
| LAY-04 | 临时详情执行页 | 沟通和活跃度检查在临时详情页，左列表不导航 | worker flow tests | 日志显示执行页核对活跃度、详情触发沟通，左侧筛选/滚动/页面实例保持 | 通过 | 既有 ego 证据 |
| LAY-05 | 执行页复用与关闭 | 可复用安全执行 tab；完成/失败后关闭或清理，不积累脏页 | worker tests | 既有真实任务日志出现临时执行页创建/复用，停止后当前 ego 空间无遗留 worker tab，任务 execution 不保留 workerTabId | 通过 | 2026-09-11 ego + worker cleanup logs |
| LAY-06 | 消息页防卡顿刷新 | 达到阈值后只刷新消息页，重新套用分屏尺寸，不刷新列表 | background flow tests | 独立边界页读取实际后台刷新计数、消息页 reload 和 `listTabId` 保护契约；flow 同时覆盖分屏尺寸重套 | 通过 | 2026-09-11 ego task 160 `boundary-audit.html` + flow；未为此项额外发送大量消息 |
| LAY-07 | 页面导航恢复 | BOSS SPA 或详情/聊天导航后能恢复安全列表/会话，不误投 | navigation/recovery tests | 独立边界页实际调用安全列表 URL 回退和 `CHAT_TRIGGERED` navigation recovery，验证详情页不误当列表 | 通过 | 2026-09-11 ego task 160 `boundary-audit.html` + flow；未切断真实投递中的页面 |
| LAY-08 | 操作超时/取消 | 页面操作统一预算，取消/超时有终态，迟到结果不能覆盖停止 | operation tests | 独立边界页实际验证过期扫描预算为 0、取消门禁调用 settle；面板真实交互验证暂停/停止终态 | 通过 | 2026-09-11 ego task 160 `boundary-audit.html` + panel harness + flow |

### 5.9 日志、记录、配置和更新

| ID | 功能点 | 期望 | 单测/冒烟 | 独立浏览器场景 | 状态 | 证据/问题 |
|---|---|---|---|---|---|---|
| DATA-01 | 实时日志 | 扫描、筛选、点击、回执、跳过和失败按时间显示 | log-order/flow tests | 诊断后实时日志保留日期时间和任务/消息回执顺序 | 通过 | 既有 ego + 2026-09-11 ego |
| DATA-02 | 详细调试日志开关 | 默认关闭；开启后记录事件级数据，仅本会话保存 | debug-log tests | 开启后诊断，导出接口返回 `enabled=true`、版本、runner、task 和 sessionOnly 元数据；随后关闭 | 通过 | 2026-09-11 ego |
| DATA-03 | 调试日志导出 | 导出 JSON 包含版本、环境、任务摘要和日志，不泄漏无关存储 | debug export tests | 读取调试导出结构，包含版本/环境/task/logs；未开启时入口隐藏 | 通过 | 2026-09-11 ego；导出未发送到外部服务 |
| DATA-04 | 日志复制/清空 | 复制为稳定时间序，清空后 UI 和本地日志为空 | log tests | 点击复制提示“日志已复制”；备份后清空为 0，再恢复原日志 | 通过 | 2026-09-11 ego |
| DATA-05 | 投递历史 | 成功/跳过/失败记录可见，任务完成时计数一致 | history tests | 记录页显示 40 条成功历史；真实受控任务成功计数与历史写入一致 | 通过 | 既有 ego + 2026-09-11 ego |
| DATA-06 | 历史状态筛选 | all/success/skipped/failed 等筛选只影响显示 | history-view tests | `success` 显示 40 条，`failed` 显示 0 条，恢复 `all` 后 40 条 | 通过 | 2026-09-11 ego |
| DATA-07 | 历史日期筛选 | 起止日期含边界；非法/反向日期不误删数据 | history date tests | 今日筛选显示 1 条；反向日期自动归一到开始日；清除筛选恢复 40 条 | 通过 | 2026-09-11 ego |
| DATA-08 | 历史导出 | 无筛选导出全部，有筛选只导出当前可见记录 | history export tests | 拦截下载链接并解析 JSON：全量 40 条、`success` 筛选 40 条，文件名分别带/不带 `-filtered`；随后清除筛选 | 通过 | 2026-09-11 ego |
| DATA-09 | 历史清空保护 | 二次确认；只清历史，不清每日统计和防重复台账 | clear-history tests | 模拟取消确认后 40 条不变；确认后历史为 0、每日统计不变，随后恢复备份 | 通过 | 2026-09-11 ego；未丢失真实历史 |
| DATA-10 | 配置导出 | 导出筛选、模板、设置、名单、简历、历史和任务数据，含版本元数据 | config round-trip tests | 导出结构包含 13 个配置字段、`version=1` 和日志上限 100；敏感数据提示存在 | 通过 | 2026-09-11 ego |
| DATA-11 | 配置导入 | 导入校验/清洗，恢复可用配置和版本兼容，不覆盖错误数据 | config import tests | 临时导入设置后回读并恢复；`null` 坏配置返回“无效配置文件”且原数据不变 | 通过 | 2026-09-11 ego |
| DATA-12 | 本地持久化 | 面板/BOSS 刷新不丢配置、任务检查点和模板；只在实际编辑时保存模板 | storage/autosave tests | 设置、筛选、消息模板、图片方案分别保存并重载回读；取消扫描保留任务检查点 | 通过 | 2026-09-11 ego；service worker 重启仍由 flow contract 覆盖 |
| DATA-13 | 清理与配额 | 日志、统计保留策略和简历存储超限给明确提示 | storage tests | 独立边界页实际验证 legacy 设置清理、计时归一化和导出日志上限 100；真实 ego 已验证单图超过 8 MB 拒绝并保持存储为空 | 通过 | 2026-09-11 ego task 160 `boundary-audit.html` + 既有真实配额测试；未构造 40 MB 大体积数据 |
| DATA-14 | 检查更新 | 读取 releases Atom，正确区分已是最新/有新版/失败 | update check tests | 设置页返回“已是最新版本（v1.7.36）”，按钮恢复可用 | 通过 | 2026-09-11 ego；GitHub Release Atom |
| DATA-15 | 项目主页/Release 链接 | 链接指向预期仓库或 Release，不吞掉当前表单 | update UI tests | 检查链接分别指向仓库主页和 `releases/tag/v1.7.36`，均为新窗口 | 通过 | 2026-09-11 ego |
| DATA-16 | 隐私与权限边界 | 配置/简历本地保存；无作者服务上传；只声明必要 BOSS 权限 | manifest/privacy review | 图片导入/重载回读只写 `bht_resumes`，未出现作者服务请求；manifest 仅声明 BOSS、GitHub 更新源和本地存储/窗口权限 | 通过 | 2026-09-11 ego + manifest review |
| DATA-17 | 多面板配置竞态保护 | 旧浮窗不能用缓存覆盖另一面板刚保存的设置；冲突时阻止投递并允许重试 | autosave/config concurrency tests | 一个面板保留未保存定时草稿，另一个面板保存详细日志开关；旧面板批量投递被拦截，提示 `配置已在其他面板更新（settings）`，历史仍为 48、runner 未启动；随后两面板均恢复默认设置 | 通过 | 2026-09-11 ego；真实跨面板回归 |

### 5.10 明确不在当前版本的能力

以下不是待修 bug，但必须通过边界检查，避免用户误以为已经支持：

| ID | 能力 | 当前结论 | 边界验证 |
|---|---|---|---|
| SCOPE-01 | 多套完整求职方案（筛选+模板+简历+限额一键切换） | 规划项，当前只有多图片简历方案与绑定规则 | 面板没有完整方案切换入口，记录为不适用 |
| SCOPE-02 | 投递漏斗/已读/回复/面试统计 | 规划项，当前记录的是本地投递状态和每日计数 | 记录页不显示平台漏斗字段，记录为不适用 |
| SCOPE-03 | 语义/向量匹配 | 未实现 | 筛选只按确定性规则，记录为不适用 |
| SCOPE-04 | AI 自动回复 | 明确不做全自动敏感信息回复 | 消息页无 AI 生成入口，记录为不适用 |
| SCOPE-05 | 自动跟进提醒 | 规划项 | 面板无跟进提醒入口，记录为不适用 |
| SCOPE-06 | 自动点击 BOSS 原生发简历 | 明确不支持，平台双向沟通门禁由用户处理 | 观察真实页面无自动点击，记录为不适用 |
| SCOPE-07 | 验证码/风控/每日上限绕过 | 明确不支持 | 触发平台限制只能停止并提示，不进行绕过测试 |

## 6. 执行顺序

按以下顺序执行可降低真实副作用：

1. `ENT-*`：入口、连接、版本和面板基础。
2. `FIL-*`、`SRC-*`：先用扫描预览验证规则、求职期望和来源恢复。
3. `DATA-*` 中不发送消息的配置、日志和历史场景。
4. `MSG-*`、`RES-*`：先在面板和规划层验证，最后才做最多 1 份真实发送回执。
5. `RUN-*`、`LAY-*`：在本次上限为 1 的条件下验证控制、执行隔离和刷新保护。
6. `SCOPE-*`：记录边界，防止把规划项当成回归失败。

每个编号完成后立即填写状态和证据；发现问题时新增“复现步骤、实际结果、根因、修复提交、回归编号”，修复后不得只依赖单测通过。

## 7. 本轮发现与修复

### 2026-09-11：刷新合并期间面板显示临时批次

- 复现：在真实 `运维/技术支持(广州)` 任务上执行刷新，后台存储已从 175 合并到 570 条，但浮窗仍显示刷新临时批次 450 条、成功数回到 0；不重开浮窗无法看到合并结果。
- 根因：`refreshAndContinue` 调用 `runPreview` 时，临时扫描任务先通过 `publishPreviewTask` 写入 storage 并广播；随后合并任务恢复原任务 ID，面板的快照门禁将这个较旧创建时间的跨 ID 快照拒绝，因此存储与面板分叉。
- 修复：刷新路径显式传递 `deferPublish: true`；`runPreview` 支持该内部标记，扫描期间不发布临时任务，合并完成后只发布原任务一次。目标模式原有的延迟发布条件保持不变。
- 回归：全新 ego 扩展实例上重复刷新，详细日志出现 `candidate_deferred_for_target_refresh`，未出现快照拒绝；面板、API 与 storage 同步从 802 合并到 1064（新增 262、重复 188），任务 ID、成功数 3 和 `运维/技术支持(广州)` 来源均保持。

## 8. 独立浏览器边界回归

- 测试页：`http://127.0.0.1:4178/tests/browser/boundary-audit.html`
- ego task space：`160`（本地 harness，不读取或修改真实账号）
- 结果：`12/12 PASS`，覆盖 TASK-04、MSG-10、RUN-06、RUN-12、RUN-13、RUN-14、RUN-15、LAY-03、LAY-06、LAY-07、LAY-08、DATA-13。
- 面板 harness 交互：暂停弹窗重试传出 `{retry:true}` 并恢复运行；定时暂停提示正确；暂停/停止终态正确；目标模式展示 `成功 1/2`，跳过不增加成功数。
- 测试桩修复：控制类状态变更现在递增 `revision/updatedAt`，避免测试桩产生与真实快照门禁不一致的假失败。

## 9. 本轮汇总

| 指标 | 数值 |
|---|---:|
| 已实现待验收功能编号 | 约 130 |
| 当前通过 | 119（含 BOSS/非 BOSS、代码/flow 子场景，以及 12 项独立边界浏览器回归） |
| 当前部分通过 | 0 |
| 当前待测 | 0 |
| 当前问题 | 0 |
| 当前阻塞 | 0（当前回归空间可稳定控制；旧 scratch 空间未作为本轮证据） |
| 当前不适用/边界项 | 7 |

本表的“约 130”是按可独立观察的用户功能和关键安全边界拆分的数量；内部纯函数不另拆浏览器编号，但必须由单元/flow contract 覆盖。

本轮补充说明：`npm test` 通过 139 个单测及完整 flow contract，`npm run smoke` 通过。BOSS 自动招呼开关与原话术、插件消息模板、筛选、定时设置、简历方案和历史数据均已恢复。消息模板新增了“删除段落前同步 DOM 草稿”的回归保护，修复了角色/文本在重绘时被旧 state 覆盖的问题；配置保存新增逐 section 基线和跨面板冲突保护，冲突后批量/单份按钮可立即重试。配置、日志和历史测试均使用备份恢复，未留下临时方案、测试图片或临时关键词；两张临时测试图片已保存回读后清除。

本轮测试安全事件：为构造目标模式来源缺失场景，直接改写了 ego 扩展存储，而已存在的浮窗内存状态和后台 runner 没有同步；测试点击因此把旧的目标模式开关切成普通批量，实际新增了 4 条成功沟通记录。随后通过 `STOP_TASK` 终止 runner，核对无迟到新增历史，真实历史从 44 条变为 48 条；之后的跨面板冲突回归未新增历史，runner 保持未运行。当前任务已停止并保留 11 个未投岗位，来源仍为 `运维/技术支持(广州)`，定时投递已关闭，工作日时段恢复为 `09:00–12:00 / 14:00–17:00`。该事件是测试夹具绕过正常 UI/消息协议造成的，不作为产品正常路径缺陷，但后续不得再用直接存储改写模拟运行中任务。
